import { SafeAreaView } from 'react-native'
import TextoCentral from './(tabs)/componentes/TextoCentral'

export default props => (
    <SafeAreaView style={{flex: 1}}>
        <TextoCentral>App</TextoCentral>
    </SafeAreaView>
)